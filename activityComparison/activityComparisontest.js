'use strict';

describe('app.activityComparison module', function() {

  beforeEach(module('app.activityComparison'));

  describe('activityComparison controller', function(){

    it('should ....', inject(function($controller) {
      var activityComparisonCtrl = $controller('activityComparisonController');
      expect(activityComparisonCtrl).toBeDefined();
    }));

  });
});